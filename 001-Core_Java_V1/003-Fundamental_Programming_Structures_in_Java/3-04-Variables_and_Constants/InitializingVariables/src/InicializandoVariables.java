/**
 * 3.4.2 Initializing Variables
 * @author kolodani
 */

public class InicializandoVariables {

    private static int vacationDays;
    private static int numerito = 2;

    /*
     * podemos inicializar la variable en la misma declaracion
     * o despues, lo importante es tratar de mantener un
     * orden y declarar e inicializar la mayor cantidad
     * de variables en el principio del programa
     * y evitar llenar de variables del mismo tipo un renglon
     * como por ejemplo
     * int a, b, c, d, e, f;
     * deberiamos hacerlo en distintos renglones para mas
     * legibilidad
     */

    public static void main(String[] args) {

        vacationDays = 15;
    }
}
